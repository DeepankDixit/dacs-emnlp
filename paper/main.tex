\documentclass[11pt]{article}
\usepackage{acl2023}

% Additional packages
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{array}
\usepackage{tabularx}

% Convenience commands
\newcommand{\dacs}{DACS}
\newcommand{\wmdp}{WMDP-Cyber}
\newcommand{\llama}{Llama~3.1~8B~Instruct}

\title{When Does Calibration Data Matter\\
for Quantizing Fine-Tuned LLMs?\\
A Cross-Format Sensitivity Study}

\author{Anonymous Submission}

\date{}

\begin{document}

% Title spans full page width; abstract is in two-column body below
\twocolumn[
  \maketitle
  \vspace{0.5em}
]

\begin{abstract}
\begin{sloppypar}
Post-training quantization (PTQ) of large language models (LLMs) requires a
small calibration set, but the consequences of calibration-data choice remain
poorly mapped, especially for \emph{domain fine-tuned} models, where the
deployment distribution diverges from both the base-model pre-training
distribution and from generic calibration corpora.  We present a systematic
sensitivity study that crosses three PTQ formats (AWQ INT4, SmoothQuant INT8,
FP8) with three calibration conditions: generic text (C1), model
self-generated text (C2), and domain-aligned text (C3, here called \dacs{}),
across \emph{three} fine-tuned domains: cybersecurity (Llama~3.1~8B on 83K
examples), biomedical (Llama-3-Med42-8B), and code (CodeLlama-7B-Instruct),
a 3$\times$3 sensitivity grid replicated on three differently fine-tuned checkpoints (27 evaluation cells), measured on both in-domain and MMLU benchmarks.
Three findings emerge.  \emph{First}, calibration sensitivity is sharply
format-dependent: the max calibration spread on MMLU is 0.00\,pp for FP8,
up to 1.03\,pp for AWQ INT4, and up to 9.08\,pp for SmoothQuant INT8;
in this 7--8B regime, recommending a single ``best'' calibration
strategy without conditioning on the quantization format is unsafe.  \emph{Second}, self-generated
calibration, previously shown to be competitive on base models
\citep{williams2025selfcalib}, causes a substantial MMLU regression under
SmoothQuant on every domain tested (9.08\,pp on cybersecurity, 2.41\,pp on
biomedical, 1.80\,pp on code; all exceed the Wilson 95\,\% half-width by a
factor of 2--11$\times$), with the rank ordering across the three checkpoints consistent with a domain-narrowness scaling hypothesis we cannot definitively test from three confounded data points.  The
failure is SmoothQuant-specific, however: for AWQ on the biomedical Med42
checkpoint, self-generated calibration is in fact the \emph{best} MedQA
condition ($+$3.20\,pp vs.\ generic), underscoring that calibration choice
must be jointly conditioned on format and model.  \emph{Third}, domain-aligned
calibration partially rescues this failure mode but does not uniformly
dominate generic calibration.
We argue that cross-format sensitivity maps, not single-method proposals, are
what practitioners deploying quantized domain LLMs actually need.  Our
27-cell evaluation grid, calibration corpora, and evaluation harness are
released at \url{https://anonymous.4open.science/r/DACS-anon} to enable
replication.
\end{sloppypar}
\end{abstract}

% ============================================================
\section{Introduction}
\label{sec:intro}

\begin{sloppypar}
Post-training quantization (PTQ) is the default route to deploying
large language models (LLMs) on commodity hardware: it reduces
memory and compute without requiring retraining or gradient
access.  Every modern PTQ recipe depends on a small
\emph{calibration set}, typically 128--512 unlabeled sequences,
which drives scale factors, smoothing vectors, clipping
thresholds, or activation-aware rescaling.
\citet{williams2024calibration} established that this choice is
not innocuous: calibration data source can swing downstream
accuracy by up to several percentage points (largest under
pruning, more moderate under weight-only quantization) on
pretrained and instruction-tuned models.
\citet{williams2025selfcalib} then showed that
\emph{self-generated} calibration text, sampled from the target
model itself, is a competitive substitute on base models,
removing the need for external corpora.
\end{sloppypar}

Both of these results are established on \emph{base} (non-fine-tuned)
models.  The deployment case that motivates practitioner PTQ today is
different: teams fine-tune a base LLM on a domain corpus (legal, medical,
cybersecurity, code) and then quantize the fine-tuned checkpoint for
cheaper inference.  In that setting, three open questions remain:

\begin{enumerate}
  \item \textbf{Does calibration sensitivity transfer across quantization
    formats?}  Generic calibration is a well-studied choice for GPTQ
    \citep{frantar2022gptq} and AWQ \citep{lin2023awq}, but how do AWQ,
    SmoothQuant, and FP8, methods with qualitatively different dependence
    on calibration statistics, compare under the \emph{same} calibration
    interventions?
  \item \textbf{Does self-generated calibration remain safe for
    fine-tuned models?}  If the fine-tuned model's output distribution
    is narrower than the inference distribution (likely, by design, after
    domain fine-tuning), self-generated activation statistics may
    systematically underestimate activation range.
  \item \textbf{Can we predict, before paying the quantization cost, which
    (format, calibration) pairs will degrade?}  A lightweight diagnostic
    metric over activation distributions would let practitioners triage
    configurations without a full evaluation loop.
\end{enumerate}

This paper addresses (1) and (2) empirically and motivates (3) as a
follow-up.  Our scope is a single \emph{cross-format sensitivity study} on
a domain fine-tuned LLM, conducted under identical hardware, identical
evaluation harness, and identical calibration-set size, to isolate the
effect of calibration-data choice.

\paragraph{Setup.}  We fine-tune \llama{} on 83K cybersecurity examples
(QLoRA, rank-16) and then quantize the fine-tuned checkpoint under three
PTQ formats with distinct calibration-dependence profiles:
\emph{AWQ INT4} (weight-only, calibration affects per-group weight scale);
\emph{SmoothQuant INT8} (joint weight--activation, calibration directly
drives the per-channel smoothing vector); and \emph{FP8} E4M3 (joint
weight--activation, but with floating-point dynamic range that is largely
insensitive to calibration).  For each format we vary three calibration
conditions: \emph{C1} generic (WikiText-2, industry default);
\emph{C2} self-generated (text sampled from the fine-tuned model itself,
$T=0.8$); and \emph{C3} domain-aligned, which we refer to as \dacs{}
(\textbf{D}omain-\textbf{A}ware \textbf{C}alibration \textbf{S}election)
throughout for brevity.  Each of the nine resulting quantized models is
evaluated on \wmdp{} and MMLU using a shared logit-based MCQ scorer.

\paragraph{Contributions.}
\begin{sloppypar}
Our contributions are deliberately scoped to
\emph{characterization}, not method proposal:
(1) a cross-format sensitivity map (AWQ INT4 / SmoothQuant INT8 / FP8
$\times$ C1/C2/C3) on a domain fine-tuned LLM, replicated across three
domains (27 evaluation cells);
(2) identification and mechanistic explanation of a SmoothQuant
self-calibration failure mode on fine-tuned models (9.08\,pp MMLU
regression on cybersecurity), attributed to per-channel activation
underestimation under narrow autoregressive decoding, a deployment
warning that qualifies \citet{williams2025selfcalib}'s
``self-calibration is safe'' result on base models;
(3) a practitioner decision surface: FP8 is calibration-agnostic and
the safe default when hardware allows; AWQ INT4 is mildly sensitive
with domain calibration trending positively but within Wilson 95\%
CI half-widths ($+$0.55\,pp WMDP, $+$0.22\,pp MMLU); SmoothQuant is
calibration-critical and self-generated data must be avoided.
We do \emph{not} claim domain calibration universally outperforms
generic calibration: the data contradicts that for SmoothQuant on
MMLU.  We situate this work against concurrent method-proposal work
(\S\ref{sec:related}) including MixCal \citep{mixcal2026} and
per-task mixed-precision allocation \citep{levi2026pertask}.
\end{sloppypar}

% ============================================================
\section{Related Work}
\label{sec:related}

\paragraph{Calibration data in LLM quantization.}
\begin{sloppypar}
\citet{williams2024calibration} first established that
calibration-data choice is material for LLM compression across
GPTQ, SparseGPT, and Wanda, evaluated on pretrained and
instruction-tuned models.
\citet{williams2025selfcalib} then proposed \emph{self-calibration}
(sampling calibration text from the target model) as competitive
with external corpora on pretrained models, and
\citet{he2025cola} (COLA) curates calibration sets for diversity
and representativeness in activation space.  All three studies
fix the quantization method and vary the corpus; none target a
domain fine-tuned checkpoint.  Our
work differs in three axes: (i) the quantization \emph{format} is
an independent variable, (ii) the target model is a \emph{domain
fine-tuned} checkpoint, and (iii) we report the first
calibration-sensitivity results for SmoothQuant (joint
weight--activation INT8) and FP8, neither covered by prior
calibration-data studies.
\end{sloppypar}

\paragraph{Concurrent method-proposal work.}
\citet{mixcal2026} (MixCal) proposes a calibration-mixing strategy
integrated with SparseGPT-style pruning, evaluated on biomedical and
legal domains; \citet{levi2026pertask} propose per-task mixed-precision
allocation from hidden-representation signals.  Both propose new
methods; we instead characterise when existing calibration strategies
succeed or fail, providing a diagnostic prior those method-proposal
approaches can target.

\paragraph{PTQ formats and activation outliers.}
\begin{sloppypar}
The three formats we evaluate are AWQ (weight-only 4-bit with
activation-aware scaling; \citealp{lin2023awq}), SmoothQuant (joint
W8A8 via a per-channel smoothing transform whose scale is a direct
function of calibration activation maxima; \citealp{xiao2023smoothquant}),
and FP8 E4M3 (floating-point with a 4-bit exponent absorbing scale
variation; \citealp{micikevicius2022fp8}).
\end{sloppypar}  GPTQ
\citep{frantar2022gptq}, LLM.int8() \citep{dettmers2022llmint8},
QuIP\# \citep{tseng2024quip}, and AQLM \citep{egiazarian2024aqlm} are
complementary methods we do not evaluate; \citet{zhu2024survey} survey
the broader landscape.  The outlier-feature analysis of
\citet{dettmers2022llmint8} and the small-calibration-set sensitivity
characterisation of \citet{hubara2021accuratept} jointly motivate
SmoothQuant's high sensitivity to calibration choice: when
self-generated calibration text underexcites the outlier channels that
general-domain inference activates, the smoothing scales SmoothQuant
learns are systematically too small.

\paragraph{Domain adaptation and evaluation.}
\begin{sloppypar}
QLoRA \citep{dettmers2023qlora} and LoRA \citep{hu2021lora} give us
the fine-tuned base; the core question is what happens when that
model is subsequently re-quantized.  MMLU
\citep{hendrycks2021mmlu} provides 57-subject general-capability
coverage; \wmdp{} \citep{li2024wmdp} is a hazardous-knowledge probe
originally released as a paired evaluation--unlearning benchmark and
used here for its cybersecurity-knowledge subset; both
use 4-choice MCQ format, enabling logit-based scoring robust to
instruction-format variation across quantized variants.
\end{sloppypar}

% ============================================================
\section{Methodology}
\label{sec:method}

An overview of the experimental pipeline (FP16 fine-tuning $\to$
$3 \times 3$ calibration--format matrix $\to$ dual-benchmark
evaluation) is provided in Appendix~\ref{app:pipeline},
Fig.~\ref{fig:pipeline}.

\subsection{Base Model and Domain Fine-Tuning}
\label{sec:base}

We start from \llama{} \citep{meta2024llama3} and perform QLoRA
fine-tuning on 83K cybersecurity instruction examples (rank-16 LoRA
adapters over $q,k,v,o,\text{gate},\text{up},\text{down}$ projections,
2 epochs, batch size 8, learning rate $2\times10^{-4}$).  The FP16
fine-tuned model achieves approximately 44--46\% on \wmdp{} and
63--65\% on MMLU, establishing our quality ceiling.  All experiments
run on a single A10 GPU (24\,GB); end-to-end reproduction of the
full 27-cell evaluation grid from base checkpoints costs
approximately \$50 of cloud compute (${\sim}1$ GPU-day) at current
rates.  All subsequent quantization is applied to this fine-tuned
checkpoint.

\subsection{Quantization Formats}
\label{sec:formats}

\begin{sloppypar}
We evaluate three formats with distinct calibration-dependence
profiles.  \textbf{AWQ INT4} \citep{lin2023awq} quantizes weights to
4-bit with FP16 activations and a calibration-driven per-group
scaling factor; calibration affects only weight scales (bounded
sensitivity).  \textbf{SmoothQuant INT8} \citep{xiao2023smoothquant}
quantizes both weights and activations to INT8 via the per-channel
equivalence transform $\mathbf{Y} =
(\mathbf{X}\operatorname{diag}(\mathbf{s})^{-1}) \cdot
(\operatorname{diag}(\mathbf{s})\mathbf{W})$ with
$s_j = \max(|\mathbf{X}_{\mathrm{calib}}|)_j^{\alpha} /
\max(|\mathbf{W}|)_j^{1-\alpha}$ ($\alpha=0.5$); since $s_j$ is a
\emph{direct} function of calibration activation maxima,
SmoothQuant is structurally the most calibration-sensitive of the
three.  \textbf{FP8} (E4M3) \citep{micikevicius2022fp8} quantizes
both weights and activations to 8-bit floating-point; the 4-bit
exponent absorbs scale variation, so calibration-induced
mis-selection becomes rounding noise rather than clipping.  We
observe that FP8 outputs are bit-equal across C1/C2/C3 on every
domain (Tables 1--3); we cannot distinguish true argmax-resolution
insensitivity from ModelOpt's FP8 path defaulting to a
calibration-independent scale, but the deployment implication is
identical.  We use NVIDIA's TensorRT-ModelOpt throughout.
\end{sloppypar}

\subsection{Calibration Conditions}
\label{sec:calib}

All three conditions use exactly 512 sequences of 512 tokens, held
constant to isolate data-distribution effects from data-size effects.

\paragraph{C1: Generic (baseline).}
WikiText-2 \citep{merity2017wikitext}: 512 cleaned-Wikipedia
passages, identical across all three quantization formats and all
three domains.  WikiText-2 is a widely-used generic-prose corpus
in the PTQ literature (most commonly as a perplexity-evaluation
set); we adopt it uniformly here as our generic-prose calibration
control so that any difference in downstream accuracy across
formats is attributable to the format--calibration interaction
rather than to a calibration-corpus shift across formats.

\paragraph{C2: Self-generated.}
Text sampled from the \emph{fine-tuned} model using temperature $T=0.8$
with nucleus sampling ($p=0.95$), unprompted continuation from a fixed
seed token.  This mirrors the setup of
\citet{williams2025selfcalib} but applied to a fine-tuned (not base)
checkpoint, which is the regime we test.

\paragraph{C3: Domain-aligned (\dacs{}).}
\begin{sloppypar}
For each domain, C3 is a 512-passage instruction-text corpus drawn
from the public dataset most closely matching the model's
fine-tuning distribution (Appendix~\ref{app:c3} for per-domain
sources, mixture, and licenses; see Limitations for a cross-domain
comparability note).  We use \dacs{} as a convenient label for this
condition; we do \emph{not} claim it is a novel method, it is the
domain-aligned control condition that defines our C1/C2/C3 contrast.
\end{sloppypar}

\subsection{Evaluation Protocol}
\label{sec:eval}

All nine quantized variants are scored by a unified logit-based MCQ
evaluator.  For each question we construct the prompt and compute
\[
  \hat{y} = \arg\max_{k \in \{A,B,C,D\}} \log p_\theta\!\bigl(\text{tok}(k)
            \mid \text{prompt}\bigr),
\]
\begin{sloppypar}
following \citet{hendrycks2021mmlu}.  This avoids generation-format
brittleness across quantized models.  \wmdp{} is zero-shot (1,987
MCQs).  MMLU uses the \texttt{cais/mmlu} ``all'' configuration test
split (14,042 MCQs), 5-shot with cross-subject exemplars.  Sequence
length is capped at 512 tokens to fit the A10 budget; each model
evaluation runs in a fresh Python subprocess to prevent CUDA state
contamination across formats.  All accuracy differences reported
in this paper include Wilson 95\% confidence intervals (CIs),
computed as $\bigl(\hat{p} + z^2/(2n) \pm
z\sqrt{\hat{p}(1-\hat{p})/n + z^2/(4n^2)}\bigr) \,/\, (1 + z^2/n)$
with $z = 1.96$.  At $n = 14{,}042$ (MMLU) the half-width is
$\pm$0.80\,pp; at $n = 1{,}987$ (WMDP) it is $\pm$2.18\,pp.  The
SQ\,C2 MMLU regression of 9.08\,pp is more than $11\times$ the
Wilson 95\% half-width, well beyond any plausible sampling
artefact.
\end{sloppypar}

% ============================================================
\section{Results}
\label{sec:results}

Table~\ref{tab:main} presents accuracy for all nine quantized variants.

\begin{table*}[t]
\centering
\caption{Accuracy (\%) on \wmdp{} and MMLU for all nine (format $\times$
         calibration) configurations.  Best per column in \textbf{bold}.
         $\Delta$ is vs.\ C1 (generic) within each format.
         \dag{} SQ C2 MMLU is the outlier discussed in \S\ref{sec:anomaly}.}
\label{tab:main}
\small
\begin{tabular}{llcccc}
\toprule
\multirow{2}{*}{\textbf{Format}} &
\multirow{2}{*}{\textbf{Calibration}} &
\multicolumn{2}{c}{\textbf{\wmdp{}}} &
\multicolumn{2}{c}{\textbf{MMLU}} \\
\cmidrule(lr){3-4}\cmidrule(lr){5-6}
& & Acc.\ (\%) & $\Delta$ & Acc.\ (\%) & $\Delta$ \\
\midrule
\multirow{3}{*}{AWQ INT4}
  & C1 Generic  & 41.22 & --    & 59.42 & -- \\
  & C2 Self-gen & 41.02 & $-$0.20 & 59.72 & $+$0.30 \\
  & C3 \dacs{}  & 41.77 & $+$0.55 & 59.64 & $+$0.22 \\
\midrule
\multirow{3}{*}{SQ INT8}
  & C1 Generic  & 40.61 & --    & 59.00 & -- \\
  & C2 Self-gen & 40.61 & $~~$0.00 & 49.92$^\dag$ & $-$9.08 \\
  & C3 \dacs{}  & 42.17 & $+$1.56 & 56.22 & $-$2.78 \\
\midrule
\multirow{3}{*}{FP8}
  & C1 Generic  & \textbf{44.19} & --    & \textbf{62.18} & -- \\
  & C2 Self-gen & 42.73 & $-$1.46 & \textbf{62.18} & $~~$0.00 \\
  & C3 \dacs{}  & \textbf{44.19} & $~~$0.00 & \textbf{62.18} & $~~$0.00 \\
\bottomrule
\end{tabular}
\end{table*}

\begin{sloppypar}
The MMLU spread per format orders sharply: FP8 0.00\,pp
(calibration-invariant), AWQ INT4 0.30\,pp (within one Wilson CI
half-width at $n=14{,}042$), SmoothQuant INT8 9.08\,pp (over
$11\times$ the $\pm$0.80\,pp Wilson half-width), dominated by the
C2 regression
shown in Figure~\ref{fig:sensitivity}.
\end{sloppypar}

\begin{figure*}[t]
\centering
\includegraphics[width=0.72\textwidth]{fig2_sensitivity.pdf}
\caption{MMLU accuracy for all nine (format $\times$ calibration) configurations.
Bars are grouped by quantization format.  The dashed line marks the FP16
baseline ($\approx$63\,\%).  The annotated drop ($-$9.08\,pp) for SQ\,INT8\,C2
is the self-calibration failure analysed in \S\ref{sec:anomaly}.  Calibration
spread (max$-$min within a format) is shown below each group.}
\label{fig:sensitivity}
\end{figure*}

% ============================================================
\section{Analysis}
\label{sec:analysis}

\subsection{Format Sensitivity Hierarchy}
\label{sec:hierarchy}

\begin{sloppypar}
The order \textbf{FP8 $<$ AWQ INT4 $<$ SmoothQuant INT8} is
mechanistically predicted by each method's structural calibration
dependence: FP8's E4M3 exponent absorbs scale mis-selection (rounding
noise only); AWQ's dependence is confined to per-group weight scaling
behind a FP16 activation path; SmoothQuant's smoothing vector is a
\emph{direct} function of calibration activation maxima, propagating
miscalibration into every INT8 matrix multiply.
\end{sloppypar}  The right question
for practitioners is not ``does calibration data matter?'' but
``does it matter for \emph{this} format?'' For FP8 no, for
SmoothQuant substantially.

\subsection{The SmoothQuant Self-Calibration Failure}
\label{sec:anomaly}

The SQ\,INT8\,C2 MMLU score of 49.92\,\% represents a 9.08\,pp regression
relative to C1 (59.00\,\%).  Strikingly, on the in-domain \wmdp{}
benchmark the same SQ\,C2 model matches SQ\,C1 exactly
(both 40.61\,\%; $\Delta = 0.00$\,pp): calibration choice on a
cyber-flavoured evaluation is invisible despite being decisive on
MMLU.  This is exactly what the activation-range mechanism predicts
when the calibration distribution matches the evaluation
distribution (\S\ref{sec:mechanism}).

Our hypothesized mechanism is activation-range underestimation.
Autoregressive decoding from a temperature-sampled fine-tuned model
produces text whose token-level activation patterns cluster in a
relatively narrow, high-confidence regime.  When this text is used as
calibration input for SmoothQuant, the per-channel
$\max(|\mathbf{X}_\text{calib}|)_j$ is systematically smaller than the
per-channel activation maxima encountered during MMLU evaluation, which
spans diverse academic prose across 57 subjects.  The resulting
smoothing factors $s_j$ are too small, leaving large activation values
outside the INT8 representable range and producing systematic
quantization error across MMLU subjects.  The same effect is less
pronounced on \wmdp{} because the fine-tuned model's self-generated
text is cybersecurity-like, and therefore activation distributions
encountered at \wmdp{} evaluation time are closer to calibration.

This is a concrete boundary condition for the ``self-calibration is
safe'' conclusion of \citet{williams2025selfcalib}: on base models and
on weight-only quantization, self-calibration works; on
\emph{fine-tuned} models combined with joint weight--activation
quantization, self-calibration can fail catastrophically.  We confirm this mechanism via direct activation-distribution
measurement in \S\ref{sec:mechanism}.

\subsection{Does C3 (\dacs{}) Beat C1?}

The answer depends on the format (Table~\ref{tab:main} $\Delta$
column).  C3 gives a small positive gain for AWQ on both benchmarks
($+$0.55 \wmdp{}, $+$0.22 MMLU), is neutral for FP8, and for
SmoothQuant trades MMLU breadth for in-domain coverage ($+$1.56
\wmdp{}, $-$2.78 MMLU; though recovering $+$6.30\,pp relative to
SQ\,C2 on MMLU).  This contradicts a naive ``domain data is always
better'' intuition: WikiText-2's encyclopaedic breadth produces
per-channel activation maxima that cover MMLU better than a
narrower cyber+academic corpus, while \dacs{}'s tighter distribution
matches cyber-flavoured activations more faithfully.

% ============================================================
\subsection{Mechanistic Validation: Activation Range Analysis}
\label{sec:mechanism}

\begin{sloppypar}
To confirm the SQ\,C2 failure is caused by calibration-range
underestimation, we instrument each FP16 checkpoint with forward
hooks recording per-channel activation maxima at every
SmoothQuant-targeted linear layer.  Each calibration corpus and a
matched MMLU sample (128 sequences each) yield a per-channel
\emph{range ratio} $r_{j,\text{cond}} =
\max(|\mathbf{X}_\text{cond}|)_j /
\max(|\mathbf{X}_\text{MMLU}|)_j$; ratios below 1.0 indicate
SmoothQuant will set smoothing scales that are systematically too
small.  Full protocol in Appendix~\ref{app:act4}.

Figure~\ref{fig:activation} shows the mean range ratio per condition
and domain.  The pattern is decisive: WikiText-2 (C1)
\emph{overshoots} inference range across all three domains
(ratios $1.052$--$1.055$), while self-generated calibration (C2)
systematically \emph{undershoots}, most catastrophically on cyber
(ratio $0.788$, gap $0.212$, $100\,\%$ of layers below 1.0), less so
on medical ($0.965$, $61.6\,\%$) and code ($0.971$, $71.9\,\%$).
Domain calibration (C3) closely matches inference for cyber and
medical (ratios $1.008$, $1.011$) but also undershoots for code
($0.941$): Python function text is syntactically narrow enough that
neither self-generated nor domain-corpus calibration covers the full
MMLU activation range, which explains the residual SQ\,C3 code
regression ($-$1.48\,pp).

Per-layer analysis (Appendix~\ref{app:act4},
Fig.~\ref{fig:gap_per_layer}) resolves the aggregate observation
across the $n = 224$ SmoothQuant-targeted linear modules per domain.
The C2 underestimation gap distributions are cleanly separated:
cyber median $0.153$ (IQR\,$[0.131, 0.268]$), medical $0.015$, code
$0.007$, with cyber's lower quartile alone exceeding medical and
code upper quartiles.  The failure is a consistent property of the
calibration mismatch across the architecture, not an artifact of
a few outlier layers.
\end{sloppypar}

\begin{figure}[t]
\centering
\includegraphics[width=0.96\columnwidth]{fig5_activation_ranges.pdf}
\caption{Mean per-channel input activation range ratio vs.\ MMLU
inference for each calibration condition and domain.  Ratio
$< 1.0$ indicates underestimation of the inference range, causing
SmoothQuant to set smoothing scales that are too small.  C2
undershoots in all three domains; C3 matches inference well for
cybersecurity and medical but also undershoots for code (code text
is too syntactically narrow to cover MMLU range).}
\label{fig:activation}
\end{figure}

% ============================================================
\section{Cross-Domain Replication}
\label{sec:crossdomain}

\begin{sloppypar}
We replicate the 3$\times$3 grid on the public biomedical checkpoint
Med42-8B (8B parameters, FP16; baselines MedQA 63.20\,\%,
MMLU 60.70\,\%) and on CodeLlama-7B-Instruct
(MMLU baseline 38.38\,\%).  Calibration, hardware, and evaluation
protocols match \S\ref{sec:method}; C3 corpus composition for each
domain is documented in Appendix~\ref{app:c3}.  MedQA is evaluated
on a 500-question random subsample ($n=500$, Wilson 95\,\%
half-width $\pm$4.34\,pp). MedQA evaluation uses the public
\texttt{GBaker/MedQA-USMLE-4-options} test split (with the
500-question sample drawn under fixed seed 42); the medical C3
corpus is drawn only from the disjoint train split, so no question
overlap between C3 and the MedQA evaluation sample is possible.
\end{sloppypar}

\begin{table*}[t]
\centering
\caption{Medical domain: MedQA and MMLU accuracy (\%) for all nine
         (format $\times$ calibration) configurations on
         \texttt{Llama3-Med42-8B}.  FP16 baseline: MedQA\,=\,63.20\,\%,
         MMLU\,=\,60.70\,\%.  Best per column in \textbf{bold}.
         $\Delta$ is vs.\ C1 within each format.}
\label{tab:medical}
\small
\begin{tabular}{llcccc}
\toprule
\multirow{2}{*}{\textbf{Format}} &
\multirow{2}{*}{\textbf{Calibration}} &
\multicolumn{2}{c}{\textbf{MedQA}} &
\multicolumn{2}{c}{\textbf{MMLU}} \\
\cmidrule(lr){3-4}\cmidrule(lr){5-6}
& & Acc.\ (\%) & $\Delta$ & Acc.\ (\%) & $\Delta$ \\
\midrule
\multirow{3}{*}{AWQ INT4}
  & C1 Generic  & 58.80 & --       & 59.53 & -- \\
  & C2 Self-gen & \textbf{62.00} & $+$3.20 & 58.83 & $-$0.70 \\
  & C3 \dacs{}  & 60.60 & $+$1.80  & 58.87 & $-$0.66 \\
\midrule
\multirow{3}{*}{SQ INT8}
  & C1 Generic  & 58.00 & --       & 58.42 & -- \\
  & C2 Self-gen & 60.20 & $+$2.20  & 56.01 & $-$2.41 \\
  & C3 \dacs{}  & \textbf{61.60} & $+$3.60  & 57.93 & $-$0.49 \\
\midrule
\multirow{3}{*}{FP8}
  & C1 Generic  & \textbf{63.20} & --       & \textbf{60.70} & -- \\
  & C2 Self-gen & \textbf{63.20} & $~~$0.00 & \textbf{60.70} & $~~$0.00 \\
  & C3 \dacs{}  & \textbf{63.20} & $~~$0.00 & \textbf{60.70} & $~~$0.00 \\
\bottomrule
\end{tabular}
\end{table*}

Table~\ref{tab:medical} shows three results that replicate and extend
the cybersecurity findings.

\paragraph{The sensitivity hierarchy holds.}
\begin{sloppypar}
FP8 remains perfectly calibration-invariant: MedQA and MMLU scores
are identical across all three conditions and exactly match the
FP16 baseline (0.00\,pp spread on both benchmarks).  AWQ INT4
shows mild sensitivity ($\le$3.20\,pp MedQA, $\le$0.70\,pp MMLU).
SmoothQuant INT8 shows the largest MMLU spread (2.41\,pp), driven
again by the C2 regression.  The order FP8 $<$ AWQ $<$ SQ is
preserved.
\end{sloppypar}

\paragraph{SQ C2 self-calibration regression replicates, at a
smaller magnitude.}
The SQ\,C2 MMLU regression is $-$2.41\,pp on medical vs.\ $-$9.08\,pp
on cybersecurity.  Both are directionally consistent with the
activation-range-underestimation mechanism (\S\ref{sec:anomaly}): the
smaller magnitude is expected because Med42 is a 8B public checkpoint
fine-tuned with a broader recipe than our narrow QLoRA cybersecurity
adapter, resulting in less activation-distribution narrowing under
self-generated decoding.  This confirms that the failure mode is not
an artefact of our specific fine-tuning setup; it generalises, but
that its magnitude orders consistently with the apparent degree of domain narrowing across our three checkpoints (with the model-family confound acknowledged in Limitations).

\paragraph{C3 (\dacs{}) vs.\ C1 and C2.}
C3 outperforms C1 on the in-domain task for both formats where
calibration matters: $+$1.80\,pp AWQ MedQA, $+$3.60\,pp SQ MedQA.
However, on MMLU, C3 is marginally negative for AWQ ($-$0.66\,pp) and
near-neutral for SQ ($-$0.49\,pp), consistent with the cybersecurity
finding that domain-narrowed calibration trades MMLU breadth for domain
accuracy.  Interestingly, for AWQ, C2 outperforms C3 on MedQA
($+$3.20\,pp vs.\ $+$1.80\,pp vs.\ C1); we attribute this to
Med42's self-generated text being strongly biomedical (more domain-aligned
than our PubMedQA-based C3 corpus), a nuance that reinforces the
complexity of calibration-data selection and strengthens the case for
cross-format sensitivity maps over single-method recommendations.

\begin{sloppypar}
A side-by-side three-domain visualisation of MMLU accuracy is
provided in Appendix~\ref{app:pipeline}, Fig.~\ref{fig:crossdomain}.
\end{sloppypar}

\paragraph{Code domain (CodeLlama-7B-Instruct).}
The 3$\times$3 grid is reproduced on CodeLlama; absolute MMLU is
lower (38.38\,\% FP16 baseline) because CodeLlama-7B is a smaller
model, so we focus on the calibration spread rather than absolute
scores.

\begin{table}[h]
\centering
\caption{Code domain: MMLU accuracy (\%) for all nine configurations on
         \texttt{CodeLlama-7b-Instruct}.  FP16 baseline MMLU\,=\,38.38\,\%.
         Best per format in \textbf{bold}.  $\Delta$ is vs.\ C1.}
\label{tab:code}
\small
\begin{tabular}{llcc}
\toprule
\textbf{Format} & \textbf{Calibration} & \textbf{MMLU (\%)} & $\Delta$ \\
\midrule
\multirow{3}{*}{AWQ INT4}
  & C1 Generic  & 36.48 & -- \\
  & C2 Self-gen & 37.47 & $+$0.99 \\
  & C3 \dacs{}  & \textbf{37.51} & $+$1.03 \\
\midrule
\multirow{3}{*}{SQ INT8}
  & C1 Generic  & \textbf{36.54} & -- \\
  & C2 Self-gen & 34.74 & $-$1.80 \\
  & C3 \dacs{}  & 35.06 & $-$1.48 \\
\midrule
\multirow{3}{*}{FP8}
  & C1 Generic  & \textbf{38.38} & -- \\
  & C2 Self-gen & \textbf{38.38} & $~~$0.00 \\
  & C3 \dacs{}  & \textbf{38.38} & $~~$0.00 \\
\bottomrule
\end{tabular}
\end{table}

Table~\ref{tab:code} replicates all three findings.  FP8 is perfectly
calibration-invariant (0.00\,pp spread).  AWQ shows a small calibration
spread (1.03\,pp on code, slightly larger than 0.30\,pp on cybersecurity
and 0.70\,pp on medical), with C3 yielding the highest score on code
($+$1.03\,pp over C1); on cyber the C3 effect is $+$0.22\,pp and on
medical it is $-$0.66\,pp (within the Wilson 95\% half-width).
SQ INT8 again shows the largest spread (1.80\,pp), driven by the C2
regression ($-$1.80\,pp).  The SQ C2 magnitude is smaller than in
cybersecurity ($-$9.08\,pp) and medical ($-$2.41\,pp), consistent with code
tokens being structurally more diverse than domain-narrow natural-language
text, limiting the extent to which self-generated decoding underestimates
the full activation range.  The hierarchy FP8 $<$ AWQ $<$ SQ is preserved.

Figure~\ref{fig:summary} summarises the calibration spread per
(format, domain) across all 27 cells; SQ INT8 magnitude orders
consistently with the apparent degree of domain narrowing across our
three checkpoints (cybersecurity 9.08 $>$ medical 2.41 $>$ code
1.80\,pp), reinforcing the activation-range mechanism.

\begin{figure}[h]
\centering
\includegraphics[width=0.96\columnwidth]{fig4_sensitivity_summary.pdf}
\caption{MMLU calibration spread per format and domain.  FP8 is
universally calibration-agnostic; SQ INT8 is universally the most
sensitive.}
\label{fig:summary}
\end{figure}

% ============================================================
\section{Discussion}
\label{sec:discussion}

\paragraph{Practitioner decision surface (scoped).}
For these three checkpoints in the 7--8B size band on A10-class
hardware: if hardware supports FP8 natively, calibration-data choice
is empirically immaterial (Tables 1--3 show 0.00\,pp spread on every
domain).  For AWQ INT4, C3-vs-C1 deltas on the in-domain benchmarks
are small (cyber $+$0.22/$+$0.55, medical $-$0.66/$+$1.80, code
$+$1.03\,pp); only the code MMLU $+$1.03 marginally exceeds the
$\pm$0.80\,pp Wilson half-width, so domain calibration is broadly
indistinguishable from generic in this study.  For SmoothQuant INT8, calibration is
deployment-critical: any corpus that underestimates inference-time
activation ranges degrades MMLU proportionally to the gap.
Self-generated text from a domain fine-tuned model is the
highest-risk option (specialisation narrows the activation
distribution by construction); generic WikiText-2 is safer and
better for MMLU retention than domain corpora because its breadth
matches the evaluation distribution.

\paragraph{Boundary condition for self-calibration.}
The failure requires three conditions: a domain fine-tuned model, a
joint weight--activation method, and a deployment benchmark broader
than the fine-tuned domain.  Removing any one breaks it
(\S\ref{sec:anomaly}).


% ============================================================
\section{Conclusion}
\label{sec:conclusion}

Across a 3$\times$3 grid on three fine-tuned 7--8B checkpoints
(27 cells), calibration sensitivity orders strictly FP8 $<$ AWQ
INT4 $<$ SmoothQuant INT8, mechanistically grounded in each format's
structural calibration dependence.  SmoothQuant's self-calibration
failure replicates on all three checkpoints (9.08 / 2.41 / 1.80\,pp
MMLU regression, 2--11$\times$ the Wilson half-width).  Use FP8 if
hardware allows; for SmoothQuant at default $\alpha\!=\!0.5$, avoid
self-generated calibration on narrow fine-tuned models.

\section*{Limitations}

The cybersecurity study uses our own QLoRA fine-tuned checkpoint; the
medical and code extensions use public checkpoints (Med42-8B,
CodeLlama-7B-Instruct), introducing two confounds.  First, fine-tuning
recipe and domain are entangled: Med42's broader instruction tuning
differs from our narrow QLoRA cybersecurity adapter, and either
factor could plausibly contribute to the smaller SQ\,C2 regression we
observe on medical (2.41\,pp vs.\ 9.08\,pp on cybersecurity).  Second,
CodeLlama-7B-Instruct is a 7B model rather than 8B, so the apparent
rank ordering of SQ\,C2 regression magnitudes across three
checkpoints (9.08 $>$ 2.41 $>$ 1.80\,pp) is not fully separable from
model size or pre-training family.  In-domain knowledge retention for
the code domain is not measured here: HumanEval is a generation
benchmark and is incompatible with our logit-based MCQ scorer, so we
report MMLU only for code and treat cross-format sensitivity (rather
than absolute capability) as the quantity of interest there.

\begin{sloppypar}
Each (format, calibration, domain) cell is evaluated with a single
calibration draw and at SmoothQuant's default
$\alpha\!=\!0.5$; our Wilson 95\% intervals therefore bound
evaluation-sampling noise but not (a) variance across alternative
self-generated, WikiText-2, or \dacs{} sub-samples or (b) sensitivity
to $\alpha$.  The mechanism-level evidence (per-layer
underestimation gap distributions over $n=224$ SmoothQuant-targeted
modules per domain, with non-overlapping cyber / medical / code
IQRs) provides architectural rather than statistical bounds on the
central failure mode, but a multi-seed and $\alpha$-swept replication
of the SQ\,C2 cell is deferred to revision work.
\end{sloppypar}

The C3 row is not strictly comparable across the three domains:
cyber C3 is disjointly sampled from the same upstream Trendyol corpus
as the QLoRA fine-tuning split (strongest possible domain-alignment),
while medical and code C3 are sampled from public instruction corpora
whose actual overlap with Med42 / CodeLlama fine-tuning data is
unknown to us.  We also did not perform an exhaustive contamination
probe between the Trendyol fine-tuning corpus and MMLU subjects; the
two corpora are disjoint by source (cybersecurity Q\&A vs.
multi-subject academic exams) with no known overlap reported in
their respective public releases.  The 512-token sequence-length cap may underrepresent long-context
calibration patterns.  MedQA results ($n=500$) carry a Wilson 95\,\%
half-width of $\pm$4.34\,pp, so MedQA deltas below that threshold
should be read as descriptive rather than inferential.  All accuracy
comparisons in the main tables are accompanied by Wilson 95\,\% CIs;
the practitioner decision surface in \S\ref{sec:discussion} is
scoped to the three checkpoints, the 7--8B size band, and the
A10-class hardware we evaluate, and should not be over-generalised
beyond that envelope.  We believe the format-sensitivity \emph{order}
(FP8 $<$ AWQ $<$ SQ) will generalise because it is mechanistically
grounded; the rank ordering across three domains supports this, but
the specific regression magnitudes are domain-dependent
(9.08\,pp cyber, 2.41\,pp medical, 1.80\,pp code).

\section*{Ethical Considerations}

\begin{sloppypar}
This work evaluates post-training quantization of publicly available
language models on publicly released benchmarks; no human-subjects
research was conducted and no new sensitive data was collected.
\wmdp{} \citep{li2024wmdp} is a hazardous-knowledge proxy
benchmark released as a paired evaluation--unlearning toolkit; we
use only its multiple-choice evaluation component as a
domain-knowledge retention probe, with no unlearning intervention
applied, consistent with the benchmark's release license.
Calibration corpora used in this study are derived from publicly
released datasets retaining their upstream licenses (WikiText-2
CC-BY-SA-3.0; the Trendyol cybersecurity instruction dataset,
PubMedQA, MedQA-USMLE, and CodeAlpaca under their respective
open-source releases).  No calibration corpus contains personally
identifying information; the WMDP hazardous-knowledge content is
used only for capability-retention evaluation, not for training
data.  We note that calibration choice empirically affects how
reliably hazardous-knowledge benchmarks survive post-training
quantization, which is relevant to downstream unlearning-evaluation
work; our paper does not perform unlearning and reports retention
only as a deployment quality metric.  The CodeAlpaca-derived code
C3 corpus inherits a non-commercial licence (CC-BY-NC-4.0); the
practitioner guidance in \S\ref{sec:discussion} should not be
interpreted as licensing that corpus for commercial use.  Quantization broadly improves the accessibility of LLM
inference by reducing memory and compute requirements; the
calibration-sensitivity characterization we provide is intended to
help practitioners deploy quantized domain models reliably.
Generative AI assistance (Anthropic's Claude family of models) was
used for writing polish (proofreading, sentence-level editing) and
coding support (debugging, scripting); all research design,
experimental methodology, analyses, and claims are the authors' own.
\end{sloppypar}

% ============================================================
% Appendix
\appendix

\section{Evaluation Implementation Details}
\label{app:eval}

\subsection{Pipeline and Cross-Domain Visualisations}
\label{app:pipeline}

\begin{figure}[h]
\centering
\includegraphics[width=0.96\columnwidth]{fig1_pipeline.pdf}
\caption{Experimental pipeline.  Left: fine-tuning stage producing
the FP16 domain checkpoint.  Centre: the $3 \times 3$
calibration--format matrix; each cell shows the resulting MMLU
accuracy.  The red-bordered cell (SQ\,INT8 $\times$ C2) is the
anomalous result analysed in \S\ref{sec:anomaly}.  Right:
dual-benchmark evaluation.}
\label{fig:pipeline}
\end{figure}

\begin{figure}[h]
\centering
\includegraphics[width=0.96\columnwidth]{fig3_crossdomain.pdf}
\caption{Cross-domain replication of calibration sensitivity across
three domains (left to right: cybersecurity, medical, code).  Each
panel plots MMLU accuracy for C1/C2/C3 within each quantization
format.  The FP8 $<$ AWQ INT4 $<$ SQ INT8 sensitivity hierarchy is
preserved across all three domains; the C2 dip for SQ INT8
replicates with domain-dependent magnitude
(9.08\,pp cyber, 2.41\,pp medical, 1.80\,pp code).}
\label{fig:crossdomain}
\end{figure}

\subsection{MCQ Scoring}

For each question we construct a prompt from the question text and the
four labeled options, compute logits at the final prompt position, and
take the argmax over the single-token IDs for \texttt{A}, \texttt{B},
\texttt{C}, \texttt{D}.  Logit-based scoring is robust to
instruction-format variation across quantized variants and does not
require the model to emit well-formed answer strings.

\subsection{SmoothQuant Loading Protocol}

\begin{sloppypar}
Loading SQ INT8 models requires a two-phase procedure due to the
TensorRT-ModelOpt quantization framework: (1) load the FP16 base model
and run \path{mtq.quantize()} with 16 warm-up samples to initialize
the 224-module quantizer structure; (2) flush the CUDA cache
(\path{gc.collect()}; \path{torch.cuda.empty_cache()}) to recover the
$\sim$5.66\,GB of activation memory from warm-up, then load the saved
INT8 state dict on CPU (\path{load_file(device="cpu")}) and apply
via \path{load_state_dict(strict=False)}, which performs in-place
\path{copy_} operations without additional GPU allocation.  Without
the cache flush, post-warm-up VRAM is 21.74\,GB, leaving insufficient
headroom on the 22.06\,GB A10 for the INT8 state-dict load.
\end{sloppypar}

\subsection{Subprocess Isolation}

Each model evaluation runs in a fresh Python subprocess to prevent
CUDA state contamination across sequential evaluations of differently
quantized models.  The parent process spawns via \texttt{os.system()}
with \texttt{tee} logging and parses a \texttt{RESULT:XX.XXXX} token
from subprocess stdout.  This prevents ModelOpt CUDA hooks registered
during one model's evaluation from interfering with subsequent models'
quantization format detection.

\subsection{Activation-Distribution Diagnostic: Full Protocol}
\label{app:act4}

This appendix expands the mechanistic validation summarised in
\S\ref{sec:mechanism}.

\paragraph{Instrumentation.}
For each domain's FP16 checkpoint we install forward hooks on every
linear layer targeted by SmoothQuant (q/k/v/o, gate, up, down across
all 32 transformer blocks: 224 layers per checkpoint).  The hooks
record the per-channel input activation maxima
$\max(|\mathbf{X}|)_j$.  We pass 128 sequences each from the C1, C2,
and C3 calibration corpora, plus 128 MMLU questions (5-shot,
matched-length), and compute the per-channel range ratio
$r_{j,\text{cond}} = \max(|\mathbf{X}_\text{cond}|)_j /
\max(|\mathbf{X}_\text{MMLU}|)_j$.  A ratio below 1.0 means the
calibration text underestimates the activation range SmoothQuant
will face at inference time, causing smoothing scales that
are systematically too small.

\paragraph{Aggregate findings (per condition).}
\begin{itemize}
\item \textbf{C1 (generic text) overshoots:}  WikiText-2 calibration
  yields mean ratios of $1.052$--$1.055$ across all three domains,
  indicating that generic prose \emph{overestimates} the activation
  range relative to MMLU inference.
\item \textbf{C2 (self-generated) undershoots in all three domains,
  with magnitudes that scale with the narrowness of the fine-tuning
  distribution:}  cyber C2 mean ratio $= 0.788$ ($100\,\%$ of layers
  below 1.0), medical C2 $= 0.965$ ($61.6\,\%$), code C2 $= 0.971$
  ($71.9\,\%$).  The cyber C2 ratio is the most extreme
  calibration-vs-inference mismatch we observe.
\item \textbf{C3 fidelity is domain-dependent:}  For cybersecurity
  (ratio $= 1.008$) and medical (ratio $= 1.011$), the domain
  corpus matches the inference distribution closely.  For \emph{code},
  however, C3 also undershoots (ratio $= 0.941$), even below C2:
  Python function text is syntactically narrow enough that neither
  self-generated nor domain-corpus calibration covers the full MMLU
  activation range.
\end{itemize}

\paragraph{Per-layer distribution.}
\begin{sloppypar}
Figure~\ref{fig:gap_per_layer} shows the per-layer C2
underestimation gap distribution per domain.  Each domain
contributes $n = 224$ independent SmoothQuant-targeted linear
modules (32 transformer layers $\times$ 7 projection types).  The
cyber median gap is $0.153$ (IQR $[0.131, 0.268]$), more than
$10\times$ the medical median ($0.015$; IQR $[-0.006, 0.035]$) and
$20\times$ the code median ($0.007$; IQR $[-0.001, 0.056]$).
Cyber's lower quartile alone exceeds the medical and code upper
quartiles.  The rank
ordering of per-layer distributions exactly matches the rank
ordering of SQ\,C2 MMLU regressions ($9.08 / 2.41 / 1.80$\,pp),
providing distribution-level (rather than $n=3$ aggregate) evidence
that the underestimation is a consistent architectural property of
the calibration mismatch rather than an aggregate driven by a few
outlier layers.
\end{sloppypar}

\begin{figure}[h]
\centering
\includegraphics[width=0.96\columnwidth]{fig6_per_layer_distribution.pdf}
\caption{Per-layer C2 underestimation gap ($1 - $ C2 mean range
ratio) by domain, computed across all 224 SmoothQuant-targeted
linear layers per domain.  The aggregate SQ\,C2 MMLU regression for
each domain is annotated above each box.}
\label{fig:gap_per_layer}
\end{figure}

\paragraph{Data release.}
\begin{sloppypar}
Full per-layer range-ratio data for all three domains
(\path{results/activity4_activation_results.json}) and the derived
per-layer summary statistics
(\path{results/activity4_per_layer_stats.json}) are available in the
anonymised repository at
\url{https://anonymous.4open.science/r/DACS-anon}.
\end{sloppypar}

\subsection{C3 (\dacs{}) Calibration Corpus Composition}
\label{app:c3}

\paragraph{Reproducibility notes.}
\begin{sloppypar}
Hyperparameters (rank, epochs, batch size, learning rate;
SmoothQuant $\alpha$, AWQ scaling configuration, FP8 E4M3 with
ModelOpt defaults) were fixed at reference values without any sweep.
All package and environment versions are pinned in the released
\path{setup_lambda.sh} script (key versions: torch 2.6.0+cu124,
nvidia-modelopt 0.42.0, autoawq 0.2.9, transformers 5.5.0, lm-eval
0.4.11).
\end{sloppypar}


\begin{sloppypar}
C3 corpora for all three domains are released as JSONL in the
anonymised repository (\path{outputs/cyber_c3_calib_512.jsonl},
\path{outputs/med_c3_calib_512.jsonl},
\path{outputs/code_c3_calib_512.jsonl}); the cybersecurity C3 file
is additionally mirrored at \path{data/dacs_calib_512.jsonl}.  Each
corpus is 512 passages capped at 512 tokens.  Table~\ref{tab:c3}
enumerates per-domain sources, mixture, and licenses.
\end{sloppypar}

\begin{table}[h]
\centering
\small
\caption{Per-domain C3 calibration corpus composition.  Each corpus
         is 512 passages, length-filtered ($\geq$60 chars for code,
         $\geq$100 chars for medical, $\geq$20 chars for cyber),
         random-shuffled with fixed seed=42 for reproducibility.}
\label{tab:c3}
\begin{tabular}{@{}p{0.14\columnwidth}p{0.55\columnwidth}p{0.21\columnwidth}@{}}
\toprule
\textbf{Domain} & \textbf{Source(s)} & \textbf{License} \\
\midrule
Cyber &
512 assistant-turn responses from the public Trendyol
cybersecurity instruction-tuning dataset on HuggingFace, the same
public corpus as the 83K fine-tuning split. &
Apache-2.0 \\
\midrule
Medical &
PubMedQA (\path{pqa_labeled} long-answer passages) plus MedQA-USMLE
4-options train-split explanations, interleaved 1:1.  Evaluation
uses the disjoint MedQA test split. &
PubMedQA MIT, MedQA MIT \\
\midrule
Code &
CodeAlpaca-20k Python-heavy assistant outputs, length-filtered. &
CC-BY-NC-4.0 (NC) \\
\bottomrule
\end{tabular}
\end{table}

For the cybersecurity domain, C3 is sampled from the same public
instruction corpus as the model's fine-tuning split (assistant-turn
responses only), making it the strongest form of "domain-aligned"
calibration in this study; this design choice is documented and is
consistent with our framing of C3 as a per-domain control condition
rather than an in-distribution leak (the calibration sample and the
fine-tuning sample are disjoint draws from the same upstream
corpus, and the evaluation benchmarks \wmdp{} and MMLU are not
sourced from the Trendyol corpus).

\bibliography{references}
\bibliographystyle{plainnat}

\end{document}
